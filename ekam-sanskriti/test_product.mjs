import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const envStr = fs.readFileSync('.env.local', 'utf8'); const env = Object.fromEntries(envStr.split('\n').filter(line => line.includes('=')).map(line => line.split('=').map(part => part.trim()))); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data: p } = await supabase.from('profiles').select('id').limit(1); await supabase.from('products').insert([{ artist_id: p?.[0]?.id || '00000000-0000-0000-0000-000000000000', name: 'Madhubani Handpainted Saree', description: 'Traditional Madhubani art on pure silk saree.', price: 4500, image_url: 'https://images.unsplash.com/photo-1579783902614-a3fb3927b6a5?w=800&auto=format&fit=crop', state: 'Bihar' }]).select().then(console.log).catch(console.error); } run();
